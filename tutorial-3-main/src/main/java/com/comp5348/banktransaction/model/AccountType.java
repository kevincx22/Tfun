package com.comp5348.banktransaction.model;

// defines the two possible account types
// it's used to distinguish personal and business accounts
public enum AccountType {
    PERSONAL,
    BUSINESS
}